## Selection/admixture/time-series estimation
This repository implements the method described in:

> INSERT REFERENCE

The snakemake workflow used for the analyses is in the `workflow/` directory. 
The particle filtering code is in the `src/` directory.
